#include "image_tools.h"

// Implementation moved to image_tools.cpp to avoid multiple definitions
/*
void matrix::fill(unsigned char *value) {
    if (!value) return;
    
    for (size_t i = 0; i < size(); i++) {
        arr[i] = value[0];
    }
}
*/ 